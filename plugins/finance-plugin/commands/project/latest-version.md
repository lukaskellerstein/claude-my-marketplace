---
description: Update the project and it's dependencies to the latest versions
allowed-tools: Bash, BashOutput, Edit, Read, Task, Write, Search, WebSearch, WebFetch
---

## Context

Dependencies:
!`uv pip list`

## Your task

Update all dependencies of this project to the latest versions. Build the project and update the code if needed. It can happen that with updating of libraries to the new versions, some changes was introduces in libraries.
